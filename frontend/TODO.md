# Sync Complete: Firebase + Firestore

Updated:
- firebase-config.js: app, auth, db exports
- auth.js: signUp, signIn, googleSignIn, etc. with Firestore users doc (role: 'patient')
- Login.html: Uses auth.js modules

Manual register still localStorage - next sync register.html to Firebase.
